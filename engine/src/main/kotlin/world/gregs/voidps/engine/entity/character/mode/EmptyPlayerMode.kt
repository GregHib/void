package world.gregs.voidps.engine.entity.character.mode

import world.gregs.voidps.engine.entity.character.player.Player

object EmptyPlayerMode : PlayerMode {
    override fun tick(player: Player) {
    }
}