[public_status]
format = "list"
persist = true
values = [ "on", "friends", "off", "hide" ]

[private_status]
format = "list"
persist = true
values = [ "on", "friends", "off" ]

[trade_status]
format = "list"
persist = true
values = [ "on", "friends", "off" ]

[boss_timers]
format = "boolean"
persist = true

[kill_counts]
format = "boolean"
persist = true
