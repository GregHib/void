package world.gregs.voidps.tools.icon

/* Class324 - minimal stub (missing from trimmed tree)
* See client/src/Class324.java for the full original. Only method2576 is
* needed to compile Class213's item-value overlay text call; the full
* text-rendering pipeline (aClass143_4063, method2579/method2583, etc.)
* is out of scope for the item-icon renderer.
*/

internal class Font {
    fun render(string: String?, i: Int, i_69_: Int, i_70_: Int, i_71_: Int, i_72_: Int) {
        /* empty - stubbed, see note above */
    }
}
