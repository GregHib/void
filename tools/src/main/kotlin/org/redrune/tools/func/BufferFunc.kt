package org.redrune.tools.func

/**
 * @author Tyluur <contact@kiaira.tech>
 * @since January 23, 2020 10:57 p.m.
 */
class BufferFunc {
    companion object {/*
        fun readInt(startIndex: Int, archive: ByteArray): Int {
            var index = startIndex
            return ((archive[index++] and 0xff.toByte()).toInt() shl 24) + ((archive[index++] and 0xff.toByte()).toInt() shl 16) + ((archive[index++] and 0xff.toByte()).toInt() shl 8) + (archive[index++] and 0xff.toByte())
        }*/
/*
        fun readInt(index: Int, buffer: ByteArray): Int {
            var index = index
            return buffer[index++] and 0xff shl 24 or (buffer[index++] and 0xff shl 16) or (buffer[index++] and 0xff shl 8) or (buffer[index++] and 0xff)
        }*/
    }
}
